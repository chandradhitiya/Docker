# Snake and Ladder Game (Flask Microservice)

## Description
A web-based Snake and Ladder game using Flask, Dockerized with Docker Compose.

## Setup Instructions
1. Build and run the app:
   ```bash
   docker-compose up --build
